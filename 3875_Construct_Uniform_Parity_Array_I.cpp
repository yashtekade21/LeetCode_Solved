class Solution {
public:
    bool uniformArray(vector<int>& nums1) { return true; }
};
static const auto kds = []() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    cout.tie(nullptr);
    return 0;
}();
